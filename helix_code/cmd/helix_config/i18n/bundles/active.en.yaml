# helix_config i18n bundle — active English (en) locale.
#
# Every entry maps a CONST-046-migrated hardcoded literal in
# helix_code/cmd/helix_config/main.go to a message ID resolved at
# runtime via Translator.T. helix_code wires a real
# *i18nadapter.Translator at boot; the in-package NoopTranslator
# echoes the message ID verbatim for unit tests + backward compat.
#
# Naming convention: helix_config_<area>_<short_description>. All IDs
# prefixed with helix_config_ to avoid namespace collision with other
# submodules' bundles when loaded into the same go-i18n.Bundle.
#
# Round-108 §11.4 anti-bluff sweep (2026-05-18): 10 highest-impact
# user-facing strings from the CLI command handlers migrated.
# Remaining 251 hardcoded strings tracked for future rounds — this
# round prioritises the "show / validate / export / import / backup
# / restore" success messages that end users see on every run.

helix_config_show_loaded_from:
  other: "Configuration loaded from: {{.Path}}"

helix_config_show_server_port:
  other: "Server Port: {{.Port}}"

helix_config_show_database_host:
  other: "Database Host: {{.Host}}"

helix_config_show_redis_enabled:
  other: "Redis Enabled: {{.Enabled}}"

helix_config_validate_failed_header:
  other: "Validation FAILED:"

helix_config_validate_ok:
  other: "Configuration is valid"

helix_config_export_written:
  other: "Configuration exported to: {{.Path}}"

helix_config_import_done:
  other: "Configuration imported from: {{.Path}}"

helix_config_backup_written:
  other: "Configuration backed up to: {{.Path}}"

helix_config_restore_done:
  other: "Configuration restored from: {{.Path}}"

# Round-195 §11.4 anti-bluff sweep (2026-05-19): 10 additional
# high-impact user-facing strings migrated. Targets the
# delete/reset/reload/watch/migrate/history/template/version flows —
# every line a normal CLI user sees when running these commands.
# Cumulative round-108 + round-195 coverage: 20 of ~261 strings.
# Remaining ~241 hardcoded strings tracked for future rounds.

helix_config_delete_done:
  other: "Deleted key: {{.Key}}"

helix_config_reset_confirm_required:
  other: "This will reset configuration to defaults. Use --force to confirm."

helix_config_reset_done:
  other: "Configuration reset to defaults"

helix_config_reload_done:
  other: "Configuration reloaded"

helix_config_watch_start:
  other: "Watching for configuration changes... (Press Ctrl+C to stop)"

helix_config_migrate_copied:
  other: "Configuration copied (versions match: {{.Version}}) from {{.From}} to {{.To}}"

helix_config_history_none:
  other: "No backup history found"

helix_config_history_header:
  other: "Configuration history:"

helix_config_template_applied:
  other: "Template '{{.Name}}' applied successfully"

helix_config_version_line:
  other: "helix-config version {{.Version}}"

# Round-203 §11.4 anti-bluff sweep (2026-05-19): 10 additional
# high-impact user-facing strings migrated. Targets the set/validate-
# error/watch-notification/template-list/version-build/merge/search/
# config-load-error/history-clean flows — every line a normal CLI
# user sees when running these commands.
# Cumulative round-108 + round-195 + round-203 coverage: 30 of ~261.
# Remaining ~231 hardcoded strings tracked for future rounds.

helix_config_set_done:
  other: "Set {{.Key}} = {{.Value}}"

helix_config_validate_failed_line:
  other: "Validation FAILED: {{.Error}}"

helix_config_watch_config_changed:
  other: "Config changed: {{.Name}}"

helix_config_template_list_header:
  other: "Available templates:"

helix_config_version_build_time:
  other: "Build time: {{.BuildTime}}"

helix_config_version_git_commit:
  other: "Git commit: {{.GitCommit}}"

helix_config_merge_written:
  other: "Merged configuration written to: {{.Path}}"

helix_config_search_no_results:
  other: "No results found"

helix_config_load_error:
  other: "Error reading config file: {{.Error}}"

helix_config_history_clean_start:
  other: "Cleaning old history entries..."

# Round-310 §11.4 anti-bluff sweep (2026-05-20): 14 additional
# high-impact user-facing strings migrated. Targets the
# template (show/create/update/delete/search/validate) +
# history (show/restore/compare/search) + schema
# (validate/generate/export/import) command handlers — every line
# a normal CLI user sees when running these subcommands.
# Cumulative round-108 + 195 + 203 + 310 coverage: 44 of ~261.
# Remaining ~217 hardcoded strings tracked for future rounds.

helix_config_template_show_action:
  other: "Showing template: {{.Name}}"

helix_config_template_create_action:
  other: "Creating template: {{.Name}}"

helix_config_template_update_action:
  other: "Updating template: {{.Name}}"

helix_config_template_delete_action:
  other: "Deleting template: {{.Name}}"

helix_config_template_search_action:
  other: "Searching templates: {{.Query}}"

helix_config_template_validate_action:
  other: "Validating template: {{.Name}}"

helix_config_history_show_action:
  other: "Showing history entry: {{.ID}}"

helix_config_history_restore_action:
  other: "Restoring configuration from: {{.ID}}"

helix_config_history_compare_action:
  other: "Comparing history entries: {{.First}} vs {{.Second}}"

helix_config_history_search_action:
  other: "Searching history: {{.Query}}"

helix_config_schema_validate_action:
  other: "Validating configuration: {{.File}}"

helix_config_schema_generate_action:
  other: "Generating schema..."

helix_config_schema_export_action:
  other: "Exporting schema to: {{.File}}"

helix_config_schema_import_action:
  other: "Importing schema from: {{.File}}"

# --- Round-314 §11.4 CONST-046 Phase 4 (2026-05-20): root command help
# text, global flag descriptions, benchmark output + message-helper
# prefixes migrated. 27 highest-frequency user-facing literals (root
# --help is shown on every invocation; ERROR/WARNING/DEBUG prefixes
# appear on every diagnostic line).

helix_config_root_short:
  other: "HelixCode Configuration Management CLI"

helix_config_root_long:
  other: |-
    HelixCode Configuration Management CLI

    Manage HelixCode configuration across all platforms with comprehensive
    validation, migration, and templating support.

helix_config_flag_config:
  other: "Configuration file path"

helix_config_flag_format:
  other: "Configuration format (json, yaml, toml)"

helix_config_flag_output:
  other: "Output format (json, yaml, table, pretty)"

helix_config_flag_session_id:
  other: "Session ID for tracking"

helix_config_flag_user:
  other: "User name for audit"

helix_config_flag_verbose:
  other: "Verbose output"

helix_config_flag_dry_run:
  other: "Dry run without making changes"

helix_config_flag_quiet:
  other: "Quiet mode (no output)"

helix_config_flag_no_color:
  other: "Disable color output"

helix_config_flag_interactive:
  other: "Interactive mode"

helix_config_flag_force:
  other: "Force operation without confirmation"

helix_config_flag_backup:
  other: "Create backup before making changes"

helix_config_flag_timeout:
  other: "Operation timeout"

helix_config_flag_max_retries:
  other: "Maximum number of retries"

helix_config_flag_show_secrets:
  other: "Show sensitive configuration values"

helix_config_flag_no_validate:
  other: "Skip configuration validation"

helix_config_flag_strict:
  other: "Enable strict validation mode"

helix_config_flag_pretty:
  other: "Pretty print JSON output"

helix_config_flag_sort_keys:
  other: "Sort object keys in output"

helix_config_benchmark_header:
  other: "Benchmark Results ({{.Iterations}} iterations):"

helix_config_benchmark_read:
  other: "  Read operations:  {{.Duration}} ({{.Rate}} ops/sec)"

helix_config_benchmark_write:
  other: "  Write operations: {{.Duration}} ({{.Rate}} ops/sec)"

helix_config_msg_prefix_error:
  other: "ERROR:"

helix_config_msg_prefix_warning:
  other: "WARNING:"

helix_config_msg_prefix_debug:
  other: "DEBUG:"

# --- Round-320 §11.4 CONST-046 Phase 4 (2026-05-20): subcommand help
# text (Short/Long) + per-subcommand flag descriptions for the show /
# get / set / delete / validate / export / import / backup commands.
# 52 user-facing literals — these appear on every `helix-config
# <subcommand> --help` invocation.

helix_config_cmd_show_short:
  other: "Show current configuration"

helix_config_cmd_show_long:
  other: |-
    Display the current HelixCode configuration.

    The configuration can be filtered by section or displayed in its entirety.
    Sensitive values are masked by default unless --show-secrets is used.

helix_config_flag_section_show:
  other: "Show only specific section"

helix_config_flag_masked:
  other: "Show masked sensitive values"

helix_config_flag_defaults_show:
  other: "Show default values for unset fields"

helix_config_flag_flattened:
  other: "Show configuration in flattened key-value format"

helix_config_cmd_get_short:
  other: "Get a configuration value"

helix_config_cmd_get_long:
  other: |-
    Retrieve a specific configuration value by key path.

    Examples:
      helix-config get application.name
      helix-config get server.port
      helix-config get llm.default_provider

helix_config_flag_type_show:
  other: "Show the type of the value"

helix_config_flag_source_show:
  other: "Show the source of the value"

helix_config_flag_valid:
  other: "Validate the retrieved value"

helix_config_cmd_set_short:
  other: "Set a configuration value"

helix_config_cmd_set_long:
  other: |-
    Set a specific configuration value by key path.

    The value is interpreted based on the target field type.
    Use quotes for string values containing spaces.

    Examples:
      helix-config set application.name "My App"
      helix-config set server.port 8080
      helix-config set llm.temperature 0.8

helix_config_flag_create:
  other: "Create field if it doesn't exist"

helix_config_flag_validate_set:
  other: "Validate value before setting"

helix_config_flag_type_force:
  other: "Force value type (string, int, float, bool)"

helix_config_flag_format_parse:
  other: "Value format for parsing"

helix_config_flag_backup_set:
  other: "Create backup before setting"

helix_config_flag_restart:
  other: "Restart affected services after setting"

helix_config_cmd_delete_short:
  other: "Delete a configuration value"

helix_config_cmd_delete_long:
  other: |-
    Delete a specific configuration value by key path.

    The field is reset to its default value.

    Examples:
      helix-config delete server.custom_headers
      helix-config delete llm.api_keys.test

helix_config_flag_reset_delete:
  other: "Reset to default value instead of deleting"

helix_config_flag_confirm_delete:
  other: "Require confirmation before deleting"

helix_config_flag_backup_delete:
  other: "Create backup before deleting"

helix_config_cmd_validate_short:
  other: "Validate configuration"

helix_config_cmd_validate_long:
  other: |-
    Validate the current or specified configuration file.

    All validation rules are applied and detailed error information is provided.

helix_config_flag_strict_validate:
  other: "Enable strict validation mode"

helix_config_flag_warnings:
  other: "Show validation warnings"

helix_config_flag_details_validate:
  other: "Show detailed validation information"

helix_config_flag_section_validate:
  other: "Validate only specific section"

helix_config_flag_schema_validate:
  other: "Validate against JSON schema"

helix_config_cmd_export_short:
  other: "Export configuration"

helix_config_cmd_export_long:
  other: |-
    Export the current configuration to a file.

    The configuration can be exported in various formats.

helix_config_flag_format_export:
  other: "Export format (json, yaml, toml)"

helix_config_flag_secrets_export:
  other: "Include sensitive values in export"

helix_config_flag_defaults_export:
  other: "Include default values"

helix_config_flag_comments_export:
  other: "Include comments in export"

helix_config_flag_compress_export:
  other: "Compress the exported file"

helix_config_flag_encrypt_export:
  other: "Encrypt the exported file"

helix_config_flag_password_export:
  other: "Password for encryption"

helix_config_cmd_import_short:
  other: "Import configuration"

helix_config_cmd_import_long:
  other: |-
    Import configuration from a file.

    The imported configuration is validated before being applied.

helix_config_flag_validate_import:
  other: "Validate imported configuration"

helix_config_flag_backup_import:
  other: "Create backup before import"

helix_config_flag_merge_import:
  other: "Merge with existing configuration"

helix_config_flag_force_import:
  other: "Force import even with validation errors"

helix_config_flag_from_import:
  other: "Source configuration version for migration"

helix_config_cmd_backup_short:
  other: "Create configuration backup"

helix_config_cmd_backup_long:
  other: |-
    Create a backup of the current configuration.

    The backup includes all configuration files and can be used for restoration.

# Round-329 §11.4 anti-bluff sweep (2026-05-20): cmd/helix_config
# round-3 — backup flags + restore / reset / reload / watch /
# migrate / template / history / schema / benchmark / completion /
# version / info / status / diff / merge / search command help text,
# flag descriptions, and the schema-show field descriptions.

helix_config_flag_incremental_backup:
  other: "Create incremental backup"

helix_config_flag_compress_backup:
  other: "Compress the backup"

helix_config_flag_description_backup:
  other: "Backup description"

helix_config_flag_tags_backup:
  other: "Backup tags (comma-separated)"

helix_config_flag_encrypt_backup:
  other: "Encrypt the backup"

helix_config_flag_password_backup:
  other: "Password for encryption"

helix_config_cmd_restore_short:
  other: "Restore configuration from backup"

helix_config_cmd_restore_long:
  other: |-
    Restore configuration from a previously created backup.

    The current configuration will be backed up before restoration.

helix_config_flag_validate_restore:
  other: "Validate restored configuration"

helix_config_flag_backup_restore:
  other: "Backup current configuration"

helix_config_flag_confirm_restore:
  other: "Require confirmation before restoration"

helix_config_flag_to_restore:
  other: "Restore to specific version"

helix_config_flag_merge_restore:
  other: "Merge with existing configuration"

helix_config_cmd_reset_short:
  other: "Reset configuration"

helix_config_cmd_reset_long:
  other: |-
    Reset configuration to default values.

    A specific section can be reset, or the entire configuration.

helix_config_flag_confirm_reset:
  other: "Require confirmation before reset"

helix_config_flag_backup_reset:
  other: "Create backup before reset"

helix_config_flag_template_reset:
  other: "Reset to specific template instead of defaults"

helix_config_flag_hard_reset:
  other: "Hard reset (remove all custom settings)"

helix_config_cmd_reload_short:
  other: "Reload configuration"

helix_config_cmd_reload_long:
  other: |-
    Reload configuration from disk.

    This re-reads the configuration file and applies any changes.

helix_config_flag_cache_reload:
  other: "Reload configuration cache"

helix_config_flag_watchers_reload:
  other: "Reload configuration watchers"

helix_config_flag_services_reload:
  other: "Reload affected services"

helix_config_cmd_watch_short:
  other: "Watch configuration changes"

helix_config_cmd_watch_long:
  other: |-
    Monitor configuration changes in real-time.

    Specific keys can be watched, or the entire configuration.

helix_config_flag_changes_watch:
  other: "Show value changes"

helix_config_flag_timestamps_watch:
  other: "Show change timestamps"

helix_config_flag_user_watch:
  other: "Show user who made changes"

helix_config_flag_format_watch:
  other: "Output format (table, json, yaml)"

helix_config_flag_follow_watch:
  other: "Continue watching until interrupted"

helix_config_flag_summary_watch:
  other: "Show periodic summary"

helix_config_cmd_migrate_short:
  other: "Migrate configuration"

helix_config_cmd_migrate_long:
  other: |-
    Migrate configuration to a different version.

    Migration is performed safely with automatic backups.

helix_config_flag_from_migrate:
  other: "Source version (auto-detected if not specified)"

helix_config_flag_backup_migrate:
  other: "Create backup before migration"

helix_config_flag_dry_run_migrate:
  other: "Perform dry run without making changes"

helix_config_flag_force_migrate:
  other: "Force migration even with warnings"

helix_config_flag_path_migrate:
  other: "Custom migration path"

helix_config_flag_validate_migrate:
  other: "Validate after migration"

helix_config_cmd_template_short:
  other: "Manage configuration templates"

helix_config_cmd_template_long:
  other: |-
    Manage configuration templates.

    Templates can be listed, applied, created, and managed.

helix_config_cmd_history_short:
  other: "Manage configuration history"

helix_config_cmd_history_long:
  other: |-
    Manage configuration change history.

    History can be viewed, compared, and restored.

helix_config_cmd_schema_short:
  other: "Manage configuration schema"

helix_config_cmd_schema_long:
  other: |-
    Manage configuration JSON schema.

    Schema can be generated, validated, and customized.

helix_config_cmd_benchmark_short:
  other: "Benchmark configuration operations"

helix_config_cmd_benchmark_long:
  other: "Benchmark various configuration operations for performance testing."

helix_config_flag_operation_benchmark:
  other: "Operation to benchmark (load, save, validate, transform, template)"

helix_config_flag_iterations_benchmark:
  other: "Number of iterations to run"

helix_config_flag_parallel_benchmark:
  other: "Run operations in parallel"

helix_config_flag_profile_benchmark:
  other: "Enable profiling (cpu, memory, heap)"

helix_config_flag_output_benchmark:
  other: "Output file for benchmark results"

helix_config_flag_compare_benchmark:
  other: "Compare with previous benchmark results"

helix_config_flag_warmup_benchmark:
  other: "Perform warmup iterations"

helix_config_cmd_template_list_short:
  other: "List available templates"

helix_config_cmd_template_list_long:
  other: "List all available configuration templates with their metadata."

helix_config_flag_category_template:
  other: "Filter by category"

helix_config_flag_tag_template:
  other: "Filter by tag"

helix_config_flag_search_template:
  other: "Search in templates"

helix_config_flag_sort_template:
  other: "Sort by (name, created, updated, author)"

helix_config_flag_details_template:
  other: "Show detailed template information"

helix_config_cmd_template_apply_short:
  other: "Apply configuration template"

helix_config_cmd_template_apply_long:
  other: "Apply a configuration template with optional variable substitution."

helix_config_flag_var_template:
  other: "Template variables (key=value)"

helix_config_flag_vars_file_template:
  other: "Template variables file"

helix_config_flag_backup_template:
  other: "Create backup before applying"

helix_config_flag_preview_template:
  other: "Preview changes without applying"

helix_config_flag_validate_template:
  other: "Validate applied configuration"

helix_config_flag_force_template:
  other: "Force apply even with validation errors"

helix_config_cmd_history_list_short:
  other: "List configuration history"

helix_config_cmd_history_list_long:
  other: "List configuration change history with details."

helix_config_flag_limit_history:
  other: "Maximum number of entries to show"

helix_config_flag_since_history:
  other: "Show changes since (timestamp or duration)"

helix_config_flag_until_history:
  other: "Show changes until (timestamp or duration)"

helix_config_flag_user_history:
  other: "Filter by user"

helix_config_flag_section_history:
  other: "Filter by configuration section"

helix_config_flag_sort_history:
  other: "Sort by (time, user, section)"

helix_config_flag_details_history:
  other: "Show detailed change information"

helix_config_cmd_schema_show_short:
  other: "Show configuration schema"

helix_config_cmd_schema_show_long:
  other: "Display the JSON schema for the configuration."

helix_config_flag_section_schema:
  other: "Show schema for specific section"

helix_config_flag_examples_schema:
  other: "Include example values in schema"

helix_config_flag_format_schema:
  other: "Output format (json, yaml)"

helix_config_flag_validate_schema:
  other: "Validate configuration against schema"

helix_config_cmd_completion_short:
  other: "Generate shell completion"

helix_config_cmd_completion_long:
  other: "Generate shell completion scripts for bash, zsh, fish, or powershell."

helix_config_flag_install_completion:
  other: "Install completion script"

helix_config_flag_shell_completion:
  other: "Shell type (bash, zsh, fish, powershell)"

helix_config_cmd_version_short:
  other: "Show version information"

helix_config_cmd_version_long:
  other: "Display detailed version and build information."

helix_config_flag_short_version:
  other: "Show short version only"

helix_config_flag_build_version:
  other: "Show build information"

helix_config_flag_deps_version:
  other: "Show dependency versions"

helix_config_cmd_info_short:
  other: "Show configuration information"

helix_config_cmd_info_long:
  other: "Display detailed information about the configuration system."

helix_config_flag_system_info:
  other: "Show system information"

helix_config_flag_files_info:
  other: "Show configuration file locations"

helix_config_flag_stats_info:
  other: "Show configuration statistics"

helix_config_flag_environment_info:
  other: "Show environment variables"

helix_config_cmd_status_short:
  other: "Show configuration status"

helix_config_cmd_status_long:
  other: "Display the current status of the configuration system."

helix_config_flag_watchers_status:
  other: "Show configuration watcher status"

helix_config_flag_cache_status:
  other: "Show configuration cache status"

helix_config_flag_locks_status:
  other: "Show lock status"

helix_config_flag_performance_status:
  other: "Show performance metrics"

helix_config_cmd_diff_short:
  other: "Compare configuration files"

helix_config_cmd_diff_long:
  other: "Compare two configuration files and show differences."

helix_config_flag_format_diff:
  other: "Output format (table, json, yaml, diff)"

helix_config_flag_unified_diff:
  other: "Unified diff format"

helix_config_flag_context_diff:
  other: "Context lines for diff"

helix_config_flag_color_diff:
  other: "Colorized output"

helix_config_flag_semantic_diff:
  other: "Semantic diff (understands configuration structure)"

helix_config_cmd_merge_short:
  other: "Merge configuration files"

helix_config_cmd_merge_long:
  other: "Merge two configuration files into one."

helix_config_flag_strategy_merge:
  other: "Merge strategy (override, merge, intersect)"

helix_config_flag_conflict_merge:
  other: "Conflict resolution (first, second, error)"

helix_config_flag_validate_merge_cmd:
  other: "Validate merged configuration"

helix_config_flag_base_merge:
  other: "Base file for three-way merge"

helix_config_flag_preview_merge:
  other: "Preview merge result without saving"

helix_config_cmd_search_short:
  other: "Search configuration"

helix_config_cmd_search_long:
  other: "Search configuration values by pattern or regex."

helix_config_flag_section_search:
  other: "Search in specific section"

helix_config_flag_regex_search:
  other: "Use regular expression"

helix_config_flag_case_sensitive_search:
  other: "Case sensitive search"

helix_config_flag_values_search:
  other: "Search in values"

helix_config_flag_keys_search:
  other: "Search in keys"

helix_config_flag_limit_search:
  other: "Maximum number of results"

helix_config_flag_sort_search:
  other: "Sort results (relevance, key, value)"

helix_config_cmd_template_show_short:
  other: "Show template details"

helix_config_cmd_template_create_short:
  other: "Create a new template"

helix_config_cmd_template_update_short:
  other: "Update an existing template"

helix_config_cmd_template_delete_short:
  other: "Delete a template"

helix_config_cmd_template_search_short:
  other: "Search templates"

helix_config_cmd_template_validate_short:
  other: "Validate a template"

helix_config_cmd_history_show_short:
  other: "Show history entry details"

helix_config_cmd_history_restore_short:
  other: "Restore configuration from history"

helix_config_cmd_history_compare_short:
  other: "Compare two history entries"

helix_config_cmd_history_search_short:
  other: "Search configuration history"

helix_config_cmd_history_clean_short:
  other: "Clean old history entries"

helix_config_cmd_schema_validate_short:
  other: "Validate configuration against schema"

helix_config_cmd_schema_generate_short:
  other: "Generate schema from configuration"

helix_config_cmd_schema_export_short:
  other: "Export schema to file"

helix_config_cmd_schema_import_short:
  other: "Import schema from file"

helix_config_schema_server_port:
  other: "int - Server port (default: 8080)"

helix_config_schema_server_host:
  other: "string - Server host (default: localhost)"

helix_config_schema_server_read_timeout:
  other: "duration - Read timeout (default: 30s)"

helix_config_schema_database_enabled:
  other: "bool - Enable database (default: false)"

helix_config_schema_database_host:
  other: "string - Database host"

helix_config_schema_database_port:
  other: "int - Database port (default: 5432)"

helix_config_schema_database_user:
  other: "string - Database user"

helix_config_schema_database_password:
  other: "string - Database password"

helix_config_schema_database_dbname:
  other: "string - Database name"

helix_config_schema_redis_enabled:
  other: "bool - Enable Redis (default: false)"

helix_config_schema_redis_host:
  other: "string - Redis host"

helix_config_schema_redis_port:
  other: "int - Redis port (default: 6379)"

helix_config_schema_redis_password:
  other: "string - Redis password"

helix_config_schema_auth_jwt_secret:
  other: "string - JWT signing secret (min 32 chars)"

helix_config_schema_auth_token_expiry:
  other: "duration - Token expiry time"
