# desktop i18n bundle — active English (en) locale.
#
# Every entry maps a CONST-046-migrated hardcoded literal in
# helix_code/applications/desktop/main.go to a message ID resolved
# at runtime via Translator.T. helix_code wires a real
# *i18nadapter.Translator at boot; the in-package NoopTranslator
# echoes the message ID verbatim for unit tests + backward compat.
#
# Naming convention: desktop_<area>_<short_description>. All IDs
# prefixed with desktop_ to avoid namespace collision with other
# submodules' bundles when loaded into the same go-i18n.Bundle.
#
# Round-136 §11.4 anti-bluff sweep (2026-05-18): 10 highest-impact
# UI labels migrated. Remaining hardcoded strings tracked for
# future rounds.

desktop_window_title:
  other: "HelixCode - Distributed AI Development Platform"

desktop_status_bar_default:
  other: "Ready | User: Not logged in | Session: None"

desktop_dashboard_header:
  other: "HelixCode - Distributed AI Development Platform"

desktop_projects_select_prompt:
  other: "Select a project to view details"

desktop_projects_create_header:
  other: "Create New Project:"

desktop_projects_delete_confirm:
  other: "Are you sure you want to delete this project?"

desktop_sessions_select_prompt:
  other: "Select a session to view details"

desktop_sessions_create_header:
  other: "Create New Session:"

desktop_models_available_header:
  other: "Available Models"

desktop_chat_input_placeholder:
  other: "Type your message here..."

# Round-313 §11.4 anti-bluff sweep (2026-05-20): 14 further
# user-facing GUI literals migrated (dashboard activity, tasks tab,
# LLM/model panels, settings about).

desktop_dashboard_activity_seed:
  other: "System initialized\nWorker pool started\nTask manager ready\nLLM providers loaded"

desktop_dashboard_activity_title:
  other: "Recent Activity"

desktop_tasks_description_placeholder:
  other: "Task description..."

desktop_tasks_new_label:
  other: "New Task:"

desktop_tasks_create_button:
  other: "Create Task"

desktop_common_refresh_button:
  other: "Refresh"

desktop_models_select_prompt:
  other: "Select a model to view details"

desktop_models_details_title:
  other: "Model Details"

desktop_chat_history_placeholder:
  other: "Chat history will appear here..."

desktop_chat_model_name_placeholder:
  other: "Model name (e.g., llama2)"

desktop_settings_about_text:
  other: "HelixCode Desktop Application\nVersion: 1.0.0\nDistributed AI Development Platform"

desktop_settings_about_title:
  other: "About"

# Round-351 §11.4 anti-bluff sweep (2026-05-20): 18 further
# user-facing GUI literals migrated (project/session/model detail
# templates, success dialogs, chat AI status messages, provider
# health labels, settings card titles/labels).

desktop_projects_details_template:
  other: "Name: {{.Name}}\nType: {{.Type}}\nPath: {{.Path}}\nDescription: {{.Description}}\nCreated: {{.Created}}\nBuild Command: {{.BuildCommand}}\nTest Command: {{.TestCommand}}"

desktop_sessions_details_template:
  other: "Name: {{.Name}}\nMode: {{.Mode}}\nStatus: {{.Status}}\nProject ID: {{.ProjectID}}\nDescription: {{.Description}}\nCreated: {{.Created}}\nDuration: {{.Duration}}"

desktop_models_details_template:
  other: "Name: {{.Name}}\nProvider: {{.Provider}}\nContext Size: {{.ContextSize}}\nCapabilities: {{.Capabilities}}"

desktop_common_success_title:
  other: "Success"

desktop_projects_created_success:
  other: "Project created successfully"

desktop_sessions_created_success:
  other: "Session created successfully"

desktop_sessions_controls_label:
  other: "Session Controls:"

desktop_sessions_complete_button:
  other: "Complete Session"

desktop_chat_provider_unavailable:
  other: "[AI ({{.Provider}}/{{.Model}})]: Provider '{{.Provider}}' not available or model not configured. Please configure it in Settings."

desktop_chat_llm_not_initialized:
  other: "[AI ({{.Provider}}/{{.Model}})]: LLM service not initialized. Please restart the application or check configuration."

desktop_health_checking:
  other: "Provider Health:\nChecking..."

desktop_health_no_manager:
  other: "Provider Health:\nNo LLM manager available"

desktop_health_header:
  other: "Provider Health:"

desktop_health_none_configured:
  other: "No providers configured"

desktop_settings_theme_subtitle:
  other: "Select application theme"

desktop_settings_server_title:
  other: "Server Connection"

desktop_settings_conn_test_title:
  other: "Connection Test"

desktop_settings_conn_test_body:
  other: "Server connection test would be performed here."

desktop_settings_anthropic_key_label:
  other: "Anthropic API Key:"

# Round-365 §11.4 anti-bluff sweep (2026-05-20): 24 nogui-mode CLI
# user-facing literals migrated (help body, status report labels,
# project/session command headers, error + success messages). The
# nogui CLIApp gained its own Translator seam this round
# (SetTranslator + t() helper), mirroring the aurora_os nogui
# pattern from round-327.

desktop_cli_unknown_command:
  other: "Unknown command: %s"

desktop_cli_unknown_subcommand:
  other: "Unknown subcommand: %s"

desktop_cli_help_body:
  other: |-
    HelixCode Desktop CLI (nogui mode)

    Usage: helix-desktop [command] [arguments]

    Commands:
      help          Show this help message
      status        Show system status
      projects      Manage projects (list, create, delete, set-active)
      sessions      Manage sessions (list, create, start, pause, complete)
      tasks         Manage tasks (list, create, cancel)
      workers       Manage workers (list, add, remove)
      llm           LLM operations (providers, models, chat)
      interactive   Start interactive mode

    Examples:
      helix-desktop status
      helix-desktop projects list
      helix-desktop projects create --name "MyProject" --path "/path/to/project"
      helix-desktop sessions create --project "proj_123" --name "Dev Session"
      helix-desktop tasks create --type building --desc "Build the project"
      helix-desktop llm providers
      helix-desktop interactive

    Build with GUI disabled using: go build -tags nogui

desktop_cli_status_header:
  other: "=== HelixCode System Status ==="

desktop_cli_status_workers:
  other: "Workers: %d total, %d active"

desktop_cli_status_tasks:
  other: "Tasks: %d total, %d running, %d completed"

desktop_cli_status_projects:
  other: "Projects: %d total, active: %s"

desktop_cli_status_sessions:
  other: "Sessions: %d total, %d active"

desktop_cli_status_llm_models:
  other: "LLM Models: %d available"

desktop_cli_projects_header:
  other: "=== Projects ==="

desktop_cli_no_projects:
  other: "No projects found."

desktop_cli_err_name_path_required:
  other: "Error: --name and --path are required"

desktop_cli_created_project:
  other: "Created project: %s (ID: %s)"

desktop_cli_err_project_id_required:
  other: "Error: project ID required"

desktop_cli_set_active_project:
  other: "Set active project: %s"

desktop_cli_deleted_project:
  other: "Deleted project: %s"

desktop_cli_sessions_header:
  other: "=== Sessions ==="

desktop_cli_no_sessions:
  other: "No sessions found."

desktop_cli_err_name_project_required:
  other: "Error: --name and --project are required"

desktop_cli_created_session:
  other: "Created session: %s (ID: %s)"

desktop_cli_err_session_id_required:
  other: "Error: session ID required"

desktop_cli_started_session:
  other: "Started session: %s"

desktop_cli_paused_session:
  other: "Paused session: %s"

desktop_cli_completed_session:
  other: "Completed session: %s"
