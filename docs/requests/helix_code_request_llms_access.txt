# Models access refinement and agents work

We MUST perform additional improvements, implementations, integrations and additions in order to reach proper product usability. We MUST improve ways how we can obtain access to models and to enforce as much as possible use of subagents-driven development approach.

## Before we begin

Make sure we fetch and pull the latest changes for constitution Submodule for the project and anywhere deep in hierarchy where it can be part of some of the existing Submodules! Every rule and mandatory constraint MUST BE fully taken into the account, followed, respected and applied in regular development flow and cycles!
Do review of all mandatory points and constraints agains iur project and all Submodules! Every skipping, violation, avoiding of those MUST BE corrected! We MUST use and apply them all!

## Access to models refinement

- For every provider we MUST make sure we can connect it by providing API key or HelixCode will recognize it from .env file or .api_keys file in our .bashrc or .zshrc exported. Then all providers models will be available to user - all which are really working - everything dynamically obtained and validated and verified using ours LLMsVerifier Submodule! Make sure all this properly documented once done, extended with tests, user guides, manuals and so on.

- We MUST still have access to everything obtained and prepared by HelixAgent - all providers to be exposed and models, HelixLLM, and AI debate ensemble! We MUST extend HelixAgent so it does expose every discovered provider individually and all its models that work! So under the same parent we will have all what we have already: HelixAgent's AI ensemble, exposed HelixLLM (if enabled) and then all providers and its models which are working. We MUST update all providers and APIs to the latest versions if we havent been doing a mainetenance for a while! Do deep web research so all providers we support, all bridged solutions to CLI agents and their models (Cluade Code, Qwen Code, OpenCode - with its Zen provider models), everything MUST BE up to date, fully extended, polished and exposed so users can utilize every single one of them! On top of this WE MUST support all cli_agents which we have as Submodules inside the Project using same bridge mechanisms and expose them as model options which will proxy our requests (prompts) using CLI and give us back the results + all power-features we can reach this way: all other model types (Vision, Generative and others), access to RAGs, Memory, MCPs, LSPs, ACPs, and everything possible! This MUST BE properly planned, additionally researched, extended, wired-up, covered with additional tests, full documentation, user guides and manuals, graphs, schemes, SQL and other important definitions, video courses and websites updates. Same HelixAgent root MUST BE extended for all this so we have dozens or hundreds of new items. Make sure we have proper naming which fits in the existing naming we have now! Once all done all CLI agents configurations MUST BE re-exported using our binnaries (helix agent binary itself) and installed (replacing existing CLI agents configs), then validated and verified that really does work every single one of them! All CLI agents that we have in our cli_agents (or other) directories as Submodules MUST BE fully updated by fetching and pulling the latest codebases, all recursive Submodules initialized, cloned and up to date so we can build, run and use locally all of them! Primary use of CLI agents for bridiged (proxy) access to models and their power features is to system installed version of every CLI agent, then as fallback every single CLI agent we have as Submodule. We MUST built each of them and install locally with proper Git versioning so no wrong files goes into the repositories! We MUST NOT change any codebase of CLI agents Submodules since we do not controll them!
Maybe we should fork each in our own repository (with some proper naming prefix) under vasic-digital org using CLIs for GitHub and GitLab both. Then we use these repos as Submodule and we are able to commit and push (updates to git ignore files and other minor changes). On top of this WE MUST have bash scripts which will regulalry fetch and pull main (master) branches of all CLI agents main repos, and perform merging of the latest changes to our forks! We MUST support all recursive Submodules nested inside every single CLI agent we fork and in our forks use only our versions of these Submodules. All forked Submodules CLI agents use MUST BE maintained regularly as well by fethcing and pulling the parent (original) repos and merging all new codebase changes into our repository versions! This MUST be fully automatized and whole idea added as MANDATORY for all future and current work with 3rd party owned Submodules and its 3rd party Submodule (fully recursive) depoendencies! All these ideas, whole process, and new MANDATURY RULES / CONSTRAINTS to respect this approach MUST BE up to the smallest details added into ours constitution Submodule (Constitution.md, CLAUDE.md, AGENTS.md, QWEN.md and other related relevant files). We MUST BE sure we fetch and pull all changes to constitution Submodule before we add all this into it! Once done we MUST commit and push all constitution Submodule changes to all upstreams so all other project that are using can start applying all these rules and use all bash scripts and utils we create! We MUST make reusable bash scripts for forking, updating and other work we will be doing for all 3rd party owen repos (Submodules) and all of them as reusable, decoupled, generic tools for any projects MUST BE aprt of constitution Submodule propelry palced under proper directories (scripts dir for example) and then used properly by our Project and all of ours Submodules relying on such 3rd part Submodules and other 3rd party dependencies (repositories).

## Agents work enforcements

- All work performed by HelixCode MUST BE possible to be done by multiple parallel agents which are properly coordinated! Subagents-driven development approach MUST BE used as much as possible so in parallel with main work stream we have other which will increase efficiency by doing all work we have in queue and which will not affect negatively the main work strem!

## Other ways of execution

- We MUST support all ways of execution and development: dynamic flows and all other that exist. Perfrom deep web research and obtain details and technical knowledge and source code access for all of them then incorporate each fully into the System.

## Level of incorporating

- All ways of working - subagents-driven, dynamic flow and others that we are going to support MUST BE done as separate Submodules with public repositories under vasic-development or HelixDevelopment orgs created by GitHub and GitLab CLIs. All new Submodules MUST BE incorporated into HelixCode and HelixAgent! Proper deep codebase analisys MUST BE perform so they are properly integraed (wired).

## Testing

All changes / fixes / new featires we do MUST BE covered with all supported types of the tests, Challenges and HelixQA test suites (banks) + fully autonomous QA sessions. We MUST updated HelixQA Submodule to the latest version of the codebase (fetch and pull)! We MUST cober all features, all flows, use cases and edge cases with each test type with coverage of 100%! No test can produce false result, there MUST NOT be bluff of any kind! EVERYTHING MUST BE fully covered with the tests, nothing left uncovered, skipped, avoided or ignored! As per constitution all independent agents MUST run agains work we do confirm our work!
