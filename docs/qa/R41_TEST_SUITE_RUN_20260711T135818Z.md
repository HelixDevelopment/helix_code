# R41 — Full Codebase Test-Suite Run (§11.4.40 / §11.4.169 dynamic confirmation)

**Track:** (T1/feature/helixllm-full-extension - claude3)
**Repo:** `/home/milos/Factory/projects/tools_and_research/helix_code`
**Inner module:** `helix_code/` (module `dev.helix.code`, go 1.26)
**Run mode:** READ-ONLY (no source touched; this report doc is the only write)
**HEAD at run time:** `e361214e9d60b2654dfa4df8cbfaa7b2ecdd3a59` (branch `feature/helixllm-full-extension`)
**Host:** `go version go1.26.4-X:nodwarf5 linux/amd64`, `nproc`=64, `uptime` load average at run time `1.47, 2.38, 2.50`

**Purpose:** complement the static coverage audit with REAL captured `go build` / `go vet` / `go test` output — a dynamic, wire-level confirmation that the codebase's tests genuinely execute and pass, per §11.4.40 (full-suite retest) and §11.4.169 (mandatory comprehensive test-type coverage with anti-bluff captured evidence). No claim of "green" is made anywhere in this document without the paste of the real command output that backs it (§11.4.6).

**Hard constraints honored:** `submodules/helix_agent`, `/mnt/track1`, `adb`, and files owned by other running streams (helix_llm A2A on `:18441`; the coverage-audit's own doc) were never touched. The coder service on `:18434` was left live and untouched throughout. Only `go build`, `go vet`, `go test`, and two read-only shell test-suites were executed; no infra was booted, no containers started, no files under source control were modified except this report.

---

## 1. Build (`go build ./...`)

```
cd helix_code && go build ./...
```

**Exit code:** `1`

**Captured tail (verbatim):**
```
# github.com/go-gl/gl/v2.1/gl
# [pkg-config --cflags  -- gl gl]
Package gl was not found in the pkg-config search path.
Perhaps you should add the directory containing `gl.pc'
to the PKG_CONFIG_PATH environment variable
No package 'gl' found
Package gl was not found in the pkg-config search path.
Perhaps you should add the directory containing `gl.pc'
to the PKG_CONFIG_PATH environment variable
No package 'gl' found
# github.com/go-gl/glfw/v3.3/glfw
In file included from ./glfw/src/internal.h:188,
                 from ./glfw/src/context.c:30,
                 from /home/milos/go/pkg/mod/github.com/go-gl/glfw/v3.3/glfw@v0.0.0-20240506104042-037f3cc74f2a/c_glfw.go:4:
./glfw/src/x11_platform.h:33:10: fatal error: X11/Xlib.h: No such file or directory
   33 | #include <X11/Xlib.h>
      |          ^~~~~~~~~~~~
compilation terminated.
```

**Root cause (confirmed):** the build host is missing the `gl` pkg-config package and the `X11/Xlib.h` development header. This is a pre-existing, documented environmental gap (per CLAUDE.md §3.4 "known pre-existing GUI/fyne build-host gap — X11/OpenGL headers"), not a code defect. Verified the failure is entirely confined to the GUI dependency chain:

```
$ go list -deps ./applications/desktop/... | grep -i "go-gl\|fyne"
fyne.io/fyne/v2
fyne.io/fyne/v2/internal/build
fyne.io/fyne/v2/internal/async
fyne.io/fyne/v2/internal/cache
fyne.io/fyne/v2/internal/driver
```

Packages requiring `go-gl`/`fyne`/X11: `applications/desktop`, `applications/aurora_os`, `applications/harmony_os`.

**HONEST SKIP (infra-gated, named):** `applications/desktop`, `applications/aurora_os`, `applications/harmony_os` — require `X11/Xlib.h` + `pkg-config gl` on the build host (GUI/OpenGL toolchain not installed in this environment). Not a code regression.

**Re-run excluding the 3 GUI packages** (278 remaining packages via `go list ./... | grep -v "applications/(desktop|aurora_os|harmony_os)"`):

```
$ go build $(cat nogui_pkgs.txt)
```
**Exit code:** `1` — but ALL 12 output lines are benign "no buildable Go source files" notices for test-only / build-tag-excluded directories, **zero real compile errors**:
```
dev.helix.code/benchmarks: no non-test Go files in .../benchmarks
dev.helix.code/internal/i18n_wiring: no non-test Go files in .../internal/i18n_wiring
dev.helix.code/security: no non-test Go files in .../security
dev.helix.code/test/automation: build constraints exclude all Go files in .../test/automation
dev.helix.code/test/load: no non-test Go files in .../test/load
dev.helix.code/tests/automation: no non-test Go files in .../tests/automation
dev.helix.code/tests/integration: build constraints exclude all Go files in .../tests/integration
dev.helix.code/tests/memory: no non-test Go files in .../tests/memory
dev.helix.code/tests/performance: no non-test Go files in .../tests/performance
dev.helix.code/tests/regression: no non-test Go files in .../tests/regression
dev.helix.code/tests/security: no non-test Go files in .../tests/security
dev.helix.code/tests/unit: no non-test Go files in .../tests/unit
```
Confirmed via `grep -vE "no non-test Go files|build constraints exclude all Go files"` → **empty output**. **Verdict: non-GUI build is CLEAN.**

---

## 2. Vet (`go vet ./internal/... ./cmd/...`)

```
$ go vet ./internal/... ./cmd/...
$ echo VET_EXIT=$?
VET_EXIT=0
```
**Output:** empty (0 lines). **Verdict: CLEAN.**

---

## 3. Unit + in-module tests (`go test ./internal/... ./cmd/...`)

```
cd helix_code && go test ./internal/... ./cmd/...
```
**Exit code:** `1`
**Total log captured:** 35,662 lines (`/tmp/.../scratchpad/gotest.log`, per-session scratchpad; not committed — see §11.4.53 no-versioned-build-artifacts)

### Per-package tally (parsed from the real `go test` output; `ok`/`?`/`FAIL` first-field, package-summary lines only)

| Result | Count |
|---|---|
| `ok` (PASS) | **151** |
| `?` (no test files) | **53** |
| `FAIL` | **2** |
| **Total packages exercised** | **206** |

Individual failing test functions (`--- FAIL:` lines, verbatim):
```
--- FAIL: TestRawText_EmittedByDefault (0.00s)
--- FAIL: TestBrowserActions (31.31s)
    --- FAIL: TestBrowserActions/evaluate_javascript (2.01s)
    --- FAIL: TestBrowserActions/get_element (0.00s)
    --- FAIL: TestBrowserActions/get_multiple_elements (0.00s)
    --- FAIL: TestBrowserActions/scroll_page (0.00s)
```

#### FAIL #1 — `dev.helix.code/internal/security` (0.241s) — `TestRawText_EmittedByDefault`

Captured failure:
```
2026/07/11 18:52:48 [INFO] Starting security scan for feature: noop-emit
2026/07/11 18:52:48 [INFO] No security scanners available for: noop-emit
2026/07/11 18:52:48 [INFO] Set SONARQUBE_URL/SONARQUBE_TOKEN or SNYK_TOKEN to enable security scanning
--- FAIL: TestRawText_EmittedByDefault (0.00s)
    translator_test.go:190: rec = "No security scanners configured", want contain raw ID (Noop echo)
FAIL
FAIL	dev.helix.code/internal/security	0.241s
```

**Investigation / root cause (read-only, no fix applied):**
- `git diff --stat -- internal/security/translator_test.go` → **empty** (no local/uncommitted changes to this file). `git log --oneline -3` shows the last touch was `cc339fc0` (a pure directory-rename commit, `HelixCode/ → helix_code/`) — **this failure is PRE-EXISTING in the committed tree, not introduced by this session or any in-flight track.**
- Root cause identified in `internal/security/translator.go`: `init()` installs the *real embedded-bundle translator* (resolved English prose) as `defaultTranslator` when the embed load succeeds (comment: `"nil = restore default" means "restore correct prose", not "revert to raw-key echo"`). `resetTranslator(t)` calls `SetTranslator(nil)`, which restores `defaultTranslator` — i.e. the real bundle translator, NOT `NoopTranslator{}`.
- `TestRawText_EmittedByDefault`'s docstring/assertion still assumes "with no translator wired (NoopTranslator)" emits the raw message ID verbatim. That assumption predates the HXC-097 anti-bluff fix (2026-06-15, cited in the same file) that intentionally made the package's default resolve to real prose rather than raw-key echo everywhere, not just on the CLI's WireAll path.
- **Classification:** stale test assertion left behind by an intentional, already-landed design change in the same package — a real, pre-existing, narrowly-scoped test/code mismatch. Not a build regression, not GUI-gated, not infra-gated. §11.4.120 (fix-breaks-its-own-gate) applies: the gate should be reconciled to assert the NEW (bundle-prose-by-default) behavior rather than the old raw-echo assumption — left as a finding for the owning fix track since this run is read-only.

#### FAIL #2 — `dev.helix.code/internal/tools/browser` (149.622s) — `TestBrowserActions` (4 of its subtests)

Captured failure (representative):
```
--- FAIL: TestBrowserActions/get_multiple_elements (0.00s)
    browser_test.go:259:
        	Error Trace:	.../internal/tools/browser/browser_test.go:259
        	Error:      	Received unexpected error:
        	            	context deadline exceeded
        	Test:       	TestBrowserActions/get_multiple_elements
    --- FAIL: TestBrowserActions/scroll_page (0.00s)
        browser_test.go:274:
        	Error:      	Received unexpected error:
        	            	context deadline exceeded
FAIL
FAIL	dev.helix.code/internal/tools/browser	149.622s
```

**Investigation / root cause (read-only, no fix applied):**
- `git diff --stat -- internal/tools/browser/browser_test.go` → **empty**. `git log --oneline -3` shows only `cc339fc0` (directory rename). **Pre-existing, not introduced by this session.**
- The test launches a real headless Chrome via `chromedp` and navigates to a real external URL (`https://example.com`) for each subtest, budgeted at 180s total (test file's own comment records a prior bump from 60s → 180s specifically because of `scroll_page` deadline timeouts under real-network jitter — i.e. this is a **known, previously-observed flaky/timing-sensitive area**, already tuned once by the codebase owners).
- Verified outbound network reachability from this host is fine: `curl -sS -m 5 https://example.com` → `HTTP 200`. Host load average at run time was moderate (`1.47` on 64 cores) — not obviously saturated.
- The first two subtests (`navigate to url`, `get page info`) are **not** in the failure list (they passed); failures start at `evaluate_javascript` and cascade through `get_element`, `get_multiple_elements`, `scroll_page` — consistent with cumulative per-subtest chromedp/browser-launch overhead (headless-Chrome-in-sandbox startup/render latency) eating into the shared 180s context budget faster than the test authors' last-tuned assumption, rather than a hard network outage.
- **Classification:** real, pre-existing, environment/timing-sensitive test failure specific to headless-Chrome-under-sandbox behavior on this host — not a code regression (file untouched), not attributable to any in-flight track's changes. Flagged as a genuine FAIL per §11.4.6 (no false-green claim), not silently reclassified as SKIP since the test actually ran and failed rather than being infra-absent.

### Integration-tagged tests — HONEST SKIP

```
$ grep -rl "^//go:build integration" --include="*_test.go" . | wc -l
60   # test files, across 17 packages:
cmd/cli, internal/cache, internal/database, internal/infraboot, internal/llm,
internal/llm/routing, internal/memory, internal/redis, internal/server,
internal/session, internal/verifier, test/integration, tests/ddos,
tests/integration, tests/integration/hooks, tests/integration/permissions,
tests/integration/persistence, tests/integration/worktree
```
**SKIP reason:** these 60 files require real PostgreSQL + Redis + Ollama infrastructure booted via `docker-compose.full-test.yml` / `make test-infra-up` (per the containers submodule, §11.4.76/§11.4.161). Booting that infra was judged out-of-scope for this READ-ONLY confirmation run to avoid resource contention with the concurrently-running parallel tracks explicitly called out as off-limits (helix_llm A2A on `:18441`, the coder service on `:18434`) per §11.4.119 (single-resource-owner) and §11.4.174 (shared-host process-ownership caution) — an honest SKIP-with-reason per §11.4.3, not a false PASS. These were **not run** and are not counted in the 151/53/2 tally above.

### GUI-gated tests — HONEST SKIP
`applications/desktop`, `applications/aurora_os`, `applications/harmony_os` — same X11/OpenGL build-host gap documented in §1 above prevents even compiling their test binaries. Not run; not counted above.

---

## 4. Secret-tooling suites (spot-run to confirm they pass in the current tree)

### `scripts/secret_scan_test.sh`
```
$ bash scripts/secret_scan_test.sh
...
Results: 24 passed, 0 failed
```
**Exit code:** `0`. All 24 fixture tests PASS, including both paired-mutation load-bearing-pattern checks (Google-key pattern neuter/restore, Anthropic-key pattern neuter/restore) — confirms the scanner genuinely depends on its detection patterns rather than trivially passing.

### `scripts/git_hooks/test_hooks.sh`
```
$ bash scripts/git_hooks/test_hooks.sh
...
RESULT: 43 PASS / 0 FAIL
```
**Exit code:** `0`. All 43 fixture tests PASS, including the §11.4.84 paired-mutation quiescence-check tests (broken idiom-check / restored-hook pairs) and the pre-push range-scan paired-mutation tests.

---

## 5. Summary

| Metric | Value |
|---|---|
| `go build ./...` exit | `1` (GUI-only failure; non-GUI subset clean, verified) |
| GUI packages SKIPPED (documented env gap) | 3 (`applications/desktop`, `applications/aurora_os`, `applications/harmony_os`) |
| `go vet ./internal/... ./cmd/...` exit | `0` (clean) |
| Packages tested (`go test ./internal/... ./cmd/...`) | 206 |
| PASS packages | 151 |
| No-test-file packages | 53 |
| FAIL packages | 2 (`internal/security`, `internal/tools/browser`) |
| Failing test functions | `TestRawText_EmittedByDefault`; `TestBrowserActions` (4/6 subtests) |
| Both FAILs pre-existing? | **Yes** — confirmed via `git diff`/`git log`, zero local changes to either test file |
| Integration-tagged tests (60 files / 17 pkgs) | SKIPPED — infra (postgres/redis/ollama) not booted, honest reason recorded |
| `scripts/secret_scan_test.sh` | 24/24 PASS |
| `scripts/git_hooks/test_hooks.sh` | 43/43 PASS |

### Overall verdict

**Test-health: SUBSTANTIALLY GREEN with 2 known, root-caused, pre-existing FAILs and 2 honestly-named SKIP categories.** Zero compile errors outside the documented GUI/X11 build-host gap. Zero vet findings. 151 of 153 exercised, test-bearing packages pass outright (98.7%); the 2 that fail have identified, non-speculative root causes (a stale i18n-default assertion in `internal/security`, and headless-Chrome timing sensitivity in `internal/tools/browser`), and neither was introduced or touched by this or any concurrently-running track. Both secret/git-hook governance test suites (67 total fixture checks) pass cleanly, including their paired-mutation anti-bluff checks. No infra-gated (integration) or GUI-gated packages were silently skipped without being named — every gap in this report is enumerated with a concrete reason, per §11.4.3/§11.4.6/§11.4.40/§11.4.169.

This report supersedes no prior claim; it is submitted as **dynamic, wire-level, captured-command-output evidence** complementing the separate static coverage audit referenced in the task brief.
