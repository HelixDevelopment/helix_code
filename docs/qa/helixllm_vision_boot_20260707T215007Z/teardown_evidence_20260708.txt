§11.4.119 single-resource-owner teardown — post-proof orphan cleanup
Captured: 2026-07-08 (UTC), same host as the original proof run.

Ownership verification (podman inspect helixllm_visiongen_visiongen_1):
  com.docker.compose.project = helixllm_visiongen
  com.docker.compose.project.config_files = /home/milos/Factory/projects/tools_and_research/helix_code/submodules/helix_llm/cmd/visiongen-boot/compose.vision.yml
  com.docker.compose.project.working_dir  = /home/milos/Factory/projects/tools_and_research/helix_code/submodules/helix_llm/cmd/visiongen-boot
Confirmed: this compose project resolves entirely inside this checkout — ours to tear down.

BEFORE teardown:
  podman ps:
    helixllm-coder Up 33 hours 8080/tcp, 50052/tcp
    helixllm_visiongen_visiongen_1 Up 39 minutes 0.0.0.0:18439->18439/tcp, 8080/tcp, 50052/tcp
  nvidia-smi (memory.total, memory.used, memory.free):
    32607 MiB, 24047 MiB, 8074 MiB

Command executed (harness's own single-owner down path, containers-submodule
compose.Orchestrator, §11.4.76/§11.4.119 — never a raw `podman rm`):
  cd submodules/helix_llm/cmd/visiongen-boot
  ./../visiongen-boot down compose.vision.yml helixllm_visiongen

Output:
  DOWN-OK: helixllm_visiongen visiongen (single-owner cleanup, coder untouched)

AFTER teardown:
  podman ps:
    helixllm-coder Up 33 hours 8080/tcp, 50052/tcp
  nvidia-smi (memory.total, memory.used, memory.free):
    32607 MiB, 19436 MiB, 12685 MiB
  curl -sS http://localhost:18434/health:
    {"status":"ok"}

Verdict: visiongen container removed, ~4.6 GiB VRAM released back to the
budget (matches the admit_check.txt pre-boot baseline of used=19436MiB
free=12685MiB), coder (helixllm-coder) untouched — same 33h uptime before
and after, /health still answers OK. No autonomous action taken against the
coder container at any point.
