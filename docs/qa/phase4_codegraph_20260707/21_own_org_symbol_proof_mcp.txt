=== §11.4.79 OWN-ORG SYMBOL RESOLUTION PROOF via codegraph MCP tool (mcp__codegraph__codegraph_explore) — 2026-07-07 ===
codegraph --version: 1.2.0
projectPath: /home/milos/Factory/projects/tools_and_research/helix_code
query: "admit vrambroker Broker Acquire ResolveModelCapability"

This is the UNFORGEABLE §11.4.79 proof: the codegraph MCP tool resolved symbols that live
ONLY inside own-org submodules — including `admit`, an UNEXPORTED (lowercase) Go function that
cannot exist anywhere except the one package it is declared in — returning verbatim source +
blast-radius (callers/tests), all from own-org submodule paths.

VERBATIM MCP RESPONSE (key excerpts):

Found 20 symbols across 7 files.

Blast radius — what depends on these:
- `admit` (submodules/helix_llm/internal/vrambroker/broker.go:178) — 4 callers in
  submodules/helix_llm/internal/vrambroker/broker.go; tests:
  submodules/helix_llm/internal/vrambroker/broker_test.go,
  submodules/helix_llm/internal/vrambroker/mutation_test.go
- `ResolveModelCapability` (submodules/llms_verifier/llm-verifier/capabilities/registry_resolve.go:62)
  — 4 callers; tests: submodules/llms_verifier/llm-verifier/capabilities/registry_resolve_test.go

Relationships (calls):
- Acquire → admit
- TestAdmit_TruthTable → admit
- Example_admissionMath → admit
- TestAdmit_PairedMutation → admit
- ResolveModelCapability → capabilityIsMappable
- ResolveModelCapability → probedCapabilityValue
- ResolveModelCapability → verificationResultFresh

Verbatim source returned for submodules/helix_llm/internal/vrambroker/broker.go (line-numbered),
including:
  178	func admit(free, needBytes, headroom int64) bool {
  179		if needBytes < 0 {
  180			return false
  181		}
  182		return free >= needBytes+headroom
  183	}

MODULE / ORG IDENTITY (own-org confirmation):
- submodules/helix_llm/            → module github.com/HelixDevelopment/HelixLLM   (HelixDevelopment org)
- submodules/llms_verifier/llm-verifier → module digital.vasic.llmsverifier         (vasic-digital org)

CONCLUSION: the codegraph v1.2.0 index genuinely reaches own-org submodule symbols
(§11.4.79 satisfied for own-org INCLUSION). An index that could not do this would be a
§11.4 PASS-bluff against AI agents; this proves it is not.

Corroborating CLI capture: see 20_own_org_symbol_proof_cli.txt (codegraph node admit +
codegraph query ResolveModelCapability, same file:line resolution).
